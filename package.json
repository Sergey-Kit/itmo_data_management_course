{
  "name": "generate-images",
  "version": "1.0.0",
  "scripts": {
    "generate-images": "npx mmdc -i Context.md -o hello.svg"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^10.2.4"
  }
}

